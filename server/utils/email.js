// server/utils/email.js
const sendEmail = async (to, subject, message) => {
    // This is a stub. Add your email-sending logic here using nodemailer or another service.
    console.log(`Sending email to ${to} with subject "${subject}"`);
  };
  
  module.exports = {
    sendEmail,
  };
  